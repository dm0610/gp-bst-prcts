name: Go

on: [ push, pull_request ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.16.x

    - name: Build
      run: pwd && ls -alt && cd $GIT_BRANCH_NAME && go build .

    - name: Test
      run:  pwd && ls -alt && cd $GIT_BRANCH_NAME && go test .